<%# Shared form used by both new and edit pages %>
<%= form_with model: book, html: { class: "book-form-container" } do |form| %>
    <%# Checks for validation errors %>
    <% if book.errors.any? %>
        <div>
            <h2><%= pluralize(book.errors.count, "error") %> prevented this book from saving:</h2>
            <ul>
                <% book.errors.full_messages.each do |message| %>
                    <li><%= message %></li>
                <% end %>
            </ul>
        </div>
    <% end %>

    <%# Title input %>
    <div class="book-form-field">
        <%= form.label :title %>
        <%= form.text_field :title %>
    </div>

    <%# Author Input %>
    <div class="book-form-field">
        <%= form.label :author %>
        <%= form.text_field :author %>
    </div>

    <%# Genre Input %>
    <div class="book-form-field">
        <%= form.label :genre %>
        <%= form.text_field :genre %>
    </div>

    <%# Short Description Input %>
    <div class="book-form-field">
        <%= form.label :short_description %>
        <%= form.text_field :short_description %>
    </div>

    <%# Status Input %>
    <div class="book-form-field">
        <%= form.label :status %>
        <%= form.select :status, Book::STATUSES, selected: (book.status || "Available") %>
    </div>

    <%# Submit Button %>
    <div class="book-form-submit">
        <%= form.submit %>
    </div>
<% end %>